# Never commit real keys.
# Use one shared public data portal key when the same key is approved for multiple APIs.
PUBLIC_DATA_SERVICE_KEY=

# Optional provider-specific aliases. Leave blank unless a provider issues a separate key.
SEOUL_OPEN_DATA_KEY=
INCHEON_AIRPORT_OPENAPI_KEY=
ITS_OPENAPI_KEY=
KMA_OPENAPI_KEY=
